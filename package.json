{
  "name": "@echoomegaprime/innovator-max",
  "version": "1.0.0",
  "description": "Governed Innovator Max MCP connector \u2014 propose, score, apply decisions with mission handoff.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "innovator-max": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "start": "node dist/cli.js",
    "dev": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/echoomegaprime/innovator-max.git"
  },
  "keywords": [
    "echo",
    "mcp",
    "oauth",
    "echoomegaprime",
    "innovator-max"
  ],
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
